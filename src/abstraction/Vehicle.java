package abstraction;

// add abstract to make/label it an abstract class
abstract public class Vehicle {
    // in abstract class: not everything as to be abstract, can have defined (solid/concreate value) variables/fields
    public boolean isCorporeal = true;

    // in an abstract method does not have a body
    public abstract void go();


}
